#Since I am not providing connection information, spring boot will see h2 on the test classpath and automatically
#use an in-memory h2 database.  It will also auto-create the schema based o nthe JPA entities available
spring.jpa.show-sql=true
#hibernate.transaction.manager_lookup_class=com.atomikos.icatch.jta.hibernate3.AtomikosJTATransactionFactory